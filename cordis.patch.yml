# Host activation for the DSH bundle. Add default config under `config:`;
# users override it in their DSH settings.
- insert:
    - id: dsh-plugin-template
      name: dsh-plugin-template
      config: {}
