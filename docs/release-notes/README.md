# Release notes

Optional. If `docs/release-notes/vX.Y.Z.md` exists when the tag is pushed, the Release workflow uses it as the GitHub Release body. Otherwise GitHub generates notes from commits and pull requests.
